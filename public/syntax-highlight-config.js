// https://github.com/andreruffert/syntax-highlight-element/

window.she = window.she || {};

window.she.config = {
  languages: ["clojure"],
  // tokenTypes: [],
  // languageTokens: {},
  // setup: async () => {},
  // tokenize: (text, language) => [],
};
